// components/SkipToContent.tsx
import React from 'react';
import FilterManager from '@/app/components/FilterManager';
import { filterOptions } from '@/app/data/uploadFormData';

export default async function FilterContainer() {

return (
    <div>
        <h1>User Filters</h1>
        {/* <FilterManager options={filterOptions} /> */}
    </div>
  );
};
